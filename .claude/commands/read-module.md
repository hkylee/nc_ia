# /read-module

모듈 소스를 읽어 맥락 문서를 생성한다. `/create-ia` Step 1을 단독으로 재실행.

## 사용법
```
/read-module {module_id}
```

예: `/read-module MBR`, `/read-module PRDD`

## 실행
`.claude/skills/read-module/SKILL.md` 지시에 따라 실행한다.
