# /build-tsv

화면 초안 JSON을 읽어 최종 IA TSV를 생성한다. `/create-ia` Step 4를 단독으로 재실행.

## 사용법
```
/build-tsv {module_id}
```

예: `/build-tsv MBR`, `/build-tsv PRDD`

## 실행
`.claude/skills/build-tsv/SKILL.md` 지시에 따라 실행한다.
